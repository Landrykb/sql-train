id: hidden_crop_optimization
name: "Optimize Crop Selection for Next Season"
tier: 6
domain: Farming NDVI
description: The farm manager needs to decide which crops to plant next season — analyze yield efficiency, soil conditions, and NDVI performance to make data-driven recommendations.
hidden: true
skills:
  - select
  - group
  - order
  - case
  - cte
  - join
datasets:
  - name: farming_yield
    file: farming_yield.csv
  - name: soil_data
    file: soil_data.csv
prereq_cases:
  - ndvi_overview
  - yield_by_crop
  - soil_joins
  - yield_trend
  - cte_soil
  - capstone_farm
seedQuery: |-
  -- SCENARIO: The Farm Manager asks:
  -- "Which crops should we plant more of next
  --  season? Show me yield per hectare, average
  --  NDVI health, and soil compatibility so I
  --  can make the best decision."
  SELECT 1;
solutionQuery: |-
  WITH CropPerformance AS (
    SELECT
      f.soil_type,
      f.region,
      COUNT(*) AS harvests,
      ROUND(AVG(f.yield), 2) AS avg_yield,
      ROUND(AVG(f.ndvi), 4) AS avg_ndvi,
      ROUND(AVG(s.soil_ph), 2) AS avg_soil_ph
    FROM farming_yield f
    INNER JOIN soil_data s ON f.id = s.id
    GROUP BY f.soil_type, f.region
  )
  SELECT
    soil_type,
    region,
    harvests,
    avg_yield,
    avg_ndvi,
    avg_soil_ph,
    CASE
      WHEN avg_yield > 800 AND avg_ndvi > 0.6 THEN 'Strongly Recommend'
      WHEN avg_yield > 500 AND avg_ndvi > 0.4 THEN 'Recommend'
      WHEN avg_yield > 200 THEN 'Consider'
      ELSE 'Not Recommended'
    END AS planting_recommendation
  FROM CropPerformance
  ORDER BY avg_yield DESC
  LIMIT 15;
instructions: |-
  The farm manager needs crop selection advice for next season.

  1. JOIN farming_yield with soil_data on id
  2. GROUP BY soil_type and region
  3. Calculate: harvests, avg_yield, avg_ndvi, avg_soil_ph
  4. Classify planting_recommendation:
     - Strongly Recommend: avg_yield > 800 AND avg_ndvi > 0.6
     - Recommend: avg_yield > 500 AND avg_ndvi > 0.4
     - Consider: avg_yield > 200
     - Not Recommended: everything else
  5. Order by avg_yield DESC, LIMIT 15
hints:
  - JOIN the two tables on id
  - Use AVG for yield, NDVI, and soil pH
  - Use CASE for the recommendation
  - GROUP BY soil_type and region
thoughtProcess:
  - "The farm manager wants to know which crops to plant more of. Translate to SQL: compare crop types by yield efficiency, NDVI health, and soil compatibility."
  - "CTE — CropPerformance: JOIN farming_yield with soil_data on id. GROUP BY soil_type, region to get AVG(yield), AVG(ndvi), AVG(soil_ph), and COUNT(*) as harvests."
  - "ROUND your averages for clean output: ROUND(AVG(f.yield), 2). Farm managers don't need 10 decimal places."
  - "Use CASE to create a recommendation column: high yield + high NDVI = 'Expand Planting', low yield or low NDVI = 'Reduce or Investigate', otherwise 'Maintain Current'."
  - "ORDER BY avg_yield DESC shows the best-performing crops first. The pattern: CTE for metrics → CASE for business recommendation → ORDER BY for prioritization."
  - "This is a real-world decision support query. The output directly answers: 'Plant more of X, investigate Y, keep Z the same.' That's the power of SQL in agriculture."
expected:
  - { soil_type: 'sandy_loam', region: 'North', harvests: 178, avg_yield: 3.58, avg_ndvi: 0.7, avg_soil_ph: 6.41, planting_recommendation: 'Not Recommended' }
  - { soil_type: 'clay', region: 'South', harvests: 165, avg_yield: 3.56, avg_ndvi: 0.72, avg_soil_ph: 6.54, planting_recommendation: 'Not Recommended' }
  - { soil_type: 'peat', region: 'South', harvests: 174, avg_yield: 3.54, avg_ndvi: 0.7, avg_soil_ph: 6.5, planting_recommendation: 'Not Recommended' }
  - { soil_type: 'peat', region: 'North', harvests: 146, avg_yield: 3.5, avg_ndvi: 0.7, avg_soil_ph: 6.49, planting_recommendation: 'Not Recommended' }
  - { soil_type: 'clay', region: 'East', harvests: 167, avg_yield: 3.49, avg_ndvi: 0.71, avg_soil_ph: 6.54, planting_recommendation: 'Not Recommended' }
  - { soil_type: 'sandy_loam', region: 'East', harvests: 169, avg_yield: 3.48, avg_ndvi: 0.7, avg_soil_ph: 6.49, planting_recommendation: 'Not Recommended' }
  - { soil_type: 'clay', region: 'West', harvests: 161, avg_yield: 3.47, avg_ndvi: 0.7, avg_soil_ph: 6.5, planting_recommendation: 'Not Recommended' }
  - { soil_type: 'clay', region: 'North', harvests: 169, avg_yield: 3.46, avg_ndvi: 0.7, avg_soil_ph: 6.49, planting_recommendation: 'Not Recommended' }
  - { soil_type: 'sandy_loam', region: 'West', harvests: 174, avg_yield: 3.46, avg_ndvi: 0.7, avg_soil_ph: 6.52, planting_recommendation: 'Not Recommended' }
  - { soil_type: 'peat', region: 'West', harvests: 166, avg_yield: 3.45, avg_ndvi: 0.7, avg_soil_ph: 6.46, planting_recommendation: 'Not Recommended' }
  - { soil_type: 'sandy_loam', region: 'South', harvests: 177, avg_yield: 3.41, avg_ndvi: 0.7, avg_soil_ph: 6.48, planting_recommendation: 'Not Recommended' }
  - { soil_type: 'peat', region: 'East', harvests: 154, avg_yield: 3.39, avg_ndvi: 0.69, avg_soil_ph: 6.49, planting_recommendation: 'Not Recommended' }
type: hidden
tags:
  - farming_ndvi
  - real_world
  - crop_optimization
  - case
  - cte
  - join
category: Farming NDVI
