id: basics_select
name: Inspect Sales Rows
tier: 1
domain: Business Retail
description: Retrieve top sales rows for a specific branch
skills:
  - select
  - filter
datasets:
  - name: business_retail
    file: business_retail.csv
  - name: returns
    file: returns.csv
prereq_cases: []
seedQuery: |-
  -- TODO: write query for basics_select
  SELECT 1;
solutionQuery: |-
  SELECT invoice_id, branch, total
  FROM business_retail
  WHERE branch = 'A'
  ORDER BY total DESC
  LIMIT 5;
instructions: |-
  Write a query to select the top 5 sales rows for branch A, ordered by total
  amount
hints:
  - Review the select concept in the GuideBook
  - Use WHERE to filter by branch
  - Use ORDER BY to sort by total
  - Use LIMIT to get 5 rows
thoughtProcess:
  - "Read the question carefully: you need 'top 5 sales rows for branch A, ordered by total'. Break this into parts — what columns, what filter, what order, how many rows."
  - "Start with SELECT: which columns does the output need? The instructions mention invoice_id, branch, and total. Write SELECT invoice_id, branch, total."
  - "Next, FROM: you're querying the business_retail table. Write FROM business_retail."
  - "Now the filter: 'for branch A' means WHERE branch = 'A'. String values in SQL need single quotes."
  - "Ordering: 'top 5 by total' means highest totals first. Write ORDER BY total DESC (DESC = descending = largest first)."
  - "Finally, LIMIT 5 caps the result to 5 rows. The pattern is always: SELECT → FROM → WHERE → ORDER BY → LIMIT."
schema:
  - name: business_retail
    columns:
      - name: invoice_id
        type: TEXT
      - name: branch
        type: TEXT
      - name: city
        type: TEXT
      - name: customer_type
        type: TEXT
      - name: gender
        type: TEXT
      - name: product_line
        type: TEXT
      - name: unit_price
        type: FLOAT
      - name: quantity
        type: INTEGER
      - name: tax_5
        type: FLOAT
      - name: total
        type: FLOAT
      - name: date
        type: DATE
      - name: time
        type: TEXT
      - name: payment
        type: TEXT
      - name: cogs
        type: FLOAT
      - name: gross_margin_percentage
        type: FLOAT
      - name: gross_income
        type: FLOAT
      - name: rating
        type: FLOAT
  - name: returns
    columns:
      - name: invoice_id
        type: TEXT
      - name: return_date
        type: DATE
      - name: return_reason
        type: TEXT
expected:
  - branch: A
    invoice_id: 687-47-8271
    total: 1039.29
  - branch: A
    invoice_id: 325-77-6186
    total: 951.825
  - branch: A
    invoice_id: 384-59-6655
    total: 932.337
  - branch: A
    invoice_id: 704-48-3927
    total: 931.035
  - branch: A
    invoice_id: 827-77-7633
    total: 926.9505
type: standard
tags:
  - business_retail
  - select
  - filter
category: Business Retail
