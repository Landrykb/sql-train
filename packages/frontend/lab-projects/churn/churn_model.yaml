id: churn_model
name: "Build Churn Classification Models"
domain: churn
project: "Customer Churn Prediction"
step_number: 3
tier: 2
skills: [classification, logistic_regression, random_forest, xgboost]
language: python
dataset_url: "https://www.kaggle.com/datasets/blastchar/telco-customer-churn"
description: "Train and compare classification models — Logistic Regression, Random Forest, and Gradient Boosting — to predict customer churn."
prereq_cases: [churn_features]
learning_objectives:
  - Train classification models for binary prediction
  - Interpret feature importance for business insights
  - Understand precision vs recall tradeoff for churn
sections:
  - title: "Train Multiple Classifiers"
    content: "Fit three models and compare performance."
    code: |
      from sklearn.linear_model import LogisticRegression
      from sklearn.ensemble import RandomForestClassifier, GradientBoostingClassifier
      from sklearn.metrics import classification_report

      models = {
          'Logistic Regression': LogisticRegression(max_iter=1000, random_state=42),
          'Random Forest': RandomForestClassifier(n_estimators=100, random_state=42),
          'Gradient Boosting': GradientBoostingClassifier(n_estimators=100, random_state=42),
      }

      for name, model in models.items():
          model.fit(X_train_bal, y_train_bal)
          y_pred = model.predict(X_test)
          print(f"\n=== {name} ===")
          print(classification_report(y_test, y_pred))
    explanation: "For churn, recall on the 'churn' class matters most — missing a churner costs more than a false alarm. Precision tells you how many flagged customers actually churned."
  - title: "Feature Importance"
    content: "Understand which factors drive churn predictions."
    code: |
      best = models['Gradient Boosting']
      importances = pd.Series(best.feature_importances_, index=X.columns).sort_values(ascending=True).tail(15)

      importances.plot(kind='barh', figsize=(10, 6), color='coral')
      plt.title('Top 15 Churn Predictors')
      plt.xlabel('Feature Importance')
      plt.tight_layout()
      plt.show()
    explanation: "Tenure, contract type, and monthly charges typically dominate. These insights help retention teams target the right customers with the right offers."
schema:
  - tenure
  - MonthlyCharges
  - TotalCharges
  - Churn_binary
thought_process:
  - "Train a simple baseline (Logistic Regression) first"
  - "Train ensemble models (Random Forest, Gradient Boosting) for comparison"
  - "Evaluate using classification_report for precision, recall, F1"
  - "Focus on recall for the churn class — missing churners is costly"
  - "Examine feature importance to identify top churn predictors"
solution_code: |
  from pyodide.http import open_url
  import pandas as pd
  import numpy as np
  from sklearn.model_selection import train_test_split
  from sklearn.linear_model import LogisticRegression
  from sklearn.ensemble import GradientBoostingClassifier
  from sklearn.metrics import classification_report

  df = pd.read_csv(open_url("/datasets/WA_Fn-UseC_-Telco-Customer-Churn.csv"))
  df['TotalCharges'] = pd.to_numeric(df['TotalCharges'], errors='coerce')
  df.dropna(inplace=True)
  df['Churn_binary'] = (df['Churn'] == 'Yes').astype(int)
  cat_cols = df.select_dtypes(include='object').columns.drop(['customerID', 'Churn'])
  df_encoded = pd.get_dummies(df.drop(['customerID', 'Churn'], axis=1), columns=cat_cols, drop_first=True)
  X = df_encoded.drop('Churn_binary', axis=1)
  y = df_encoded['Churn_binary']
  X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2, stratify=y, random_state=42)

  # Train models
  models = {
      'Logistic Regression': LogisticRegression(max_iter=1000, random_state=42),
      'Gradient Boosting': GradientBoostingClassifier(n_estimators=100, random_state=42),
  }
  for name, model in models.items():
      model.fit(X_train, y_train)
      y_pred = model.predict(X_test)
      print(f"\n=== {name} ===")
      print(classification_report(y_test, y_pred))
expected_output: |
  === Logistic Regression ===
  === Gradient Boosting ===
hints:
  - "Use ROC AUC score for comparing models on imbalanced data"
  - "Try adjusting the classification threshold (default 0.5) for better recall"
  - "Logistic Regression coefficients show direction: positive = increases churn probability"
visualizations:
  - title: "Model Performance Comparison"
    code: |
      import matplotlib.pyplot as plt
      model_names = ['Logistic Regression', 'Random Forest', 'Gradient Boosting']
      f1_scores = [f1_lr, f1_rf, f1_gb]
      plt.bar(model_names, f1_scores, color=['steelblue', 'coral', 'green'])
      plt.title('F1 Score Comparison')
      plt.ylabel('F1 Score')
      plt.show()
    description: "Compare F1 scores across all models"
  - title: "Feature Importance Plot"
    code: |
      import matplotlib.pyplot as plt
      importances = pd.Series(best.feature_importances_, index=X.columns).sort_values(ascending=True).tail(15)
      importances.plot(kind='barh', figsize=(10, 6), color='coral')
      plt.title('Top 15 Churn Predictors')
      plt.xlabel('Feature Importance')
      plt.tight_layout()
      plt.show()
    description: "Shows which features most strongly predict churn"
  - title: "ROC Curve Comparison"
    code: |
      import matplotlib.pyplot as plt
      from sklearn.metrics import roc_curve, auc
      for name, model in models.items():
        y_proba = model.predict_proba(X_test)[:, 1]
        fpr, tpr, _ = roc_curve(y_test, y_proba)
        plt.plot(fpr, tpr, label=f'{name} (AUC = {auc(fpr, tpr):.3f})')
      plt.plot([0, 1], [0, 1], 'k--')
      plt.xlabel('False Positive Rate')
      plt.ylabel('True Positive Rate')
      plt.title('ROC Curves')
      plt.legend()
      plt.show()
    description: "Compares ROC curves - higher AUC is better"
  - title: "Confusion Matrix Heatmap"
    code: |
      import seaborn as sns
      import matplotlib.pyplot as plt
      from sklearn.metrics import confusion_matrix
      cm = confusion_matrix(y_test, y_pred)
      sns.heatmap(cm, annot=True, fmt='d', cmap='Blues')
      plt.title('Confusion Matrix')
      plt.ylabel('True')
      plt.xlabel('Predicted')
      plt.show()
    description: "Shows true vs predicted churn classifications"
