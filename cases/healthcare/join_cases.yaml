id: join_cases
name: Cases vs Vax Join
tier: 2
domain: healthcare
description: |
  JOIN cases & vax.
skills: ['join']
dataset_key: healthcare_covid
prereq_cases: ['vax_rows', 'vax_rate']
datasets:
  - name: main
    file: healthcare_covid.csv
seedQuery: |
  -- TODO: write query for join_cases
  SELECT 1;
expected: []
hints:
  - "Review the join concept in the GuideBook."
